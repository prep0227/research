# RA-L 投稿视频分镜（≥2 分钟，无真机数据时的备选素材策略）

> 状态：**分镜草稿**。真机段落等 P3/P4 数据到位后补拍；当前可用仿真 + 架构动画 + 延迟画像工具演示。

## 结构（总时长 2:00–2:30）

### 0:00–0:20 开场：问题
- 画面：RoboMaster 对战片段（公开素材/队内录像）→ 冻结在"弹丸飞向旧位置"瞬间。
- 字幕："aiming at the current position is systematically wrong"。
- 画外音：视觉延迟链（六段）——相机曝光、检测、串口、云台、发弹、飞行。

### 0:20–0:50 方法（Fig.1 + Fig.2 动画化）
- 架构图逐块点亮：IMM 估计器 → 在线延迟估计器 → 延迟感知 MPC → 发弹决策（蓝色块）。
- 延迟链动画：六个色块随时间累积（对应 Fig.2），叠加"估计器输出 mean±Δ"柱状图。
- 字幕：三个贡献点（显式时变延迟建模 / 在线估计 / 受控消融基准）。

### 0:50–1:30 仿真对比（Fig.3 + 表）
- 三方案 B0/B1/Ours 在 4 类轨迹下的命中率柱状图逐项对比（line/circle/S/accel，drift 模式）。
- 特写：变速轨迹下 Ours 发弹点 vs B1 发弹点（命中框 vs 脱靶框）。
- 字幕：主结果（vs B0 +12~42pp，12/12 p<0.01，11/12 p<0.001；vs B1 直线/圆周/变速显著）；诚实标注 S 形 vs B1 不显著、常数时延对照 A3 仅差 1~3pp。
- 消融一段：A1（无时延建模）/A2（无预测）命中率暴跌 → 归因清晰。

### 1:30–1:50 实时性 + 开源
- 求解耗时动画：ADMM P99=4.9ms < 20ms 控制周期。
- 展示仓库：sim 复现、tools/delay_profiler、tools/replay、C++ 求解器。
- 字幕：reproducibility first（预注册、520 runs×10 seeds、开源）。

### 1:50–2:10 真机（P4 到位后替换此段；当前为占位）
- 实验台：自研步兵 + 靶标装置（旋转臂/循迹小车/摆动机构）。
- 分屏：裁判系统命中事件流 vs 离线重放（tools/replay）三方案对比。
- 字幕：预注册协议（300 发×3 轮/场景，≥5pp 且 p<0.05）。

### 2:10–2:30 收尾
- 结论三行 + 未来工作（车辆旋转模型、tube 鲁棒 MPC、嵌入式部署）。
- 结束卡：标题、作者、GitHub 链接、引用关键文献。

## 素材清单（当前已有）
- Fig.1/Fig.2 PNG（可逐块动画化）
- Fig.3/Fig.4 结果图
- sim/ 可录制实时运行（matplotlib 动画可后处理）
- tools/delay_profiler 演示（合成数据）
- tools/replay 演示（合成日志）

## 待补（真机）
- 对局/实验台视频、裁判系统命中记录、延迟画像实测曲线。
